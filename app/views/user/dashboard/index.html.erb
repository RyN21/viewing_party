
<h1>Welcome <%= @user.name %>!</h1>

<%= link_to "Discover Movies", "/user/discover" %>

<div class="friends">
  <h3>Friends</h3>
  <%= form_tag(user_path, method: "post") do %>
  Add Friend's Email: <%= email_field(:email) %>
  <%= submit_tag("Add Friends")%>
  <%end%>
</div>

<div class="viewing_parties">
  <h3>Viewing Parties</h3>
  # @viewing_parties.each do
</div>
